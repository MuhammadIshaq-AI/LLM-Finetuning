# LLM-Finetuning
What if we could take a language model... and teach it something new that it doesn't know yet? Teach your model that doesn't know about you, in this repository we will fine-tune a pretrained LLM model on our own custom dataset.
